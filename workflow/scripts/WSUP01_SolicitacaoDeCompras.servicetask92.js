function servicetask92(attempt, message) {
	return true;
	/*
	var numIdGeradoRMTemporario = hAPI.getCardValue("numIdGeradoRMTemporario");
if(numIdGeradoRMTemporario != ''){
	log.info("Inicio servicetask92 - Integracao RM processar movimento ");
	var processouMovimento = processarMovimento();
	log.info("WSUP01 - servicetask92 - Integracao RM movimento retorno servicetask20="+processouMovimento);
	if (!processouMovimento) {
		throw "WSUP01 - Ocorreu um erro inesperado na atividade servicetask92 -- Erro Processar Movimento RM. Entre em contato com a equipe de TI.";
	}
}

*/
}
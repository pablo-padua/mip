function servicetask86(attempt, message) {
	return true;
	/*
	log.info(" WSUP01 - servicetask86 - Integracao RM movimento ");

	contador = 0;
	var indexes = getIndexes("indicePaiFilhoItemRev");
	var iterator = indexes.iterator();
	while(iterator.hasNext()){
		var index = iterator.next();
		var numMovIntegracaoRev = hAPI.getCardValue("numMovIntegracaoRev___" + index);
		var qtdRev = parseInt(hAPI.getCardValue("qtdRev___" + index));
		var idMovAnterior = hAPI.getCardValue("idMovAnterior___" + index);
		
		
		log.info("WSUP01 - servicetask86 - Integracao RM movimento numMovIntegracaoRev"+numMovIntegracaoRev);
		log.info("WSUP01 - servicetask86 - Integracao RM movimento qtdRev"+qtdRev);
		log.info("WSUP01 - servicetask86 - Integracao RM movimento idMovAnterior"+idMovAnterior);
		if(numMovIntegracaoRev == '' && qtdRev > 0 && idMovAnterior != ''){
		contador++;
		}
	}
	
	log.info("WSUP01 - servicetask86 - Integracao RM movimento contador"+contador);
	if(contador > 0){
		var integrouMovimento = integrarMovimento();
		log.info("WSUP01 - servicetask86 - Integracao RM movimento integrouMovimento"+integrouMovimento);
		if (!integrouMovimento) {
			throw "WSUP01 - Ocorreu um erro inesperado na atividade servicetask86 -- Erro Integracao Movimento RM. Entre em contato com a equipe de TI.";
		}
	
	}
*/
}